<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:id="@+id/main"
    android:padding="16dp"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/img"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@id/button_gallery"
        android:layout_marginBottom="16dp"
        android:scaleType="centerCrop"
        android:contentDescription="Selected Image Placeholder"
        tools:ignore="NotSibling" />

    <LinearLayout
        android:id="@+id/button_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginBottom="16dp">

        <Button
            android:id="@+id/button_gallery"
            android:layout_width="120dp"
            android:layout_height="75dp"
            android:text="Select Image"
            android:layout_marginEnd="8dp"/>

        <Button
            android:id="@+id/button_fullsize"
            android:layout_width="120dp"
            android:layout_height="75dp"
            android:text="Take Photo (Full)"
            android:layout_marginEnd="8dp"/>

        <Button
            android:id="@+id/button_thumbnail"
            android:layout_width="120dp"
            android:layout_height="75dp"
            android:text="Take Photo (Thumb)"/>
    </LinearLayout>
</RelativeLayout>
